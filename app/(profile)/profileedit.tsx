import MandarinText from "@/components/ui/MandarinText";
import { SafeAreaView } from "react-native-safe-area-context";

export default function ProfileEdit() {
  return (
    <SafeAreaView>
      <MandarinText>ProfileEdit</MandarinText>
    </SafeAreaView>
  );
}
