import ProfileLoveTypePage from "@/components/profile/profilelovetypepage";
import { SafeAreaView } from "react-native-safe-area-context";

export default function ProfileLovetype() {
  return (
    <SafeAreaView className="flex-1 bg-white">
      <ProfileLoveTypePage />
    </SafeAreaView>
  );
}
