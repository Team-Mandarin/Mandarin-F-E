import LoveTypeTestPage from "@/components/profile/lovetypetestpage";
import { SafeAreaView } from "react-native-safe-area-context";

export default function NewLoveTypeTest() {
  return (
    <SafeAreaView className="flex-1 bg-white">
      <LoveTypeTestPage />
    </SafeAreaView>
  );
}
